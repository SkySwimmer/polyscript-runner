# The Root Script
A root PolyScript instance (eg. `build.settings.json`) - the root settings file is the file loaded by the target Tool, the root script is the main settings script and controls the base environment to work within

The root script is the primary script used by the Target Tool, and is internally assigned as the Main Script.